import "./Loading.css";

const Loading = (() => {
    return(
        <div>
            
            <div className="spinner mt-20 text-center mx-auto"></div>

            <br></br>

            <p className="font-bold text-center ">Loading...</p>
            
        </div>
    );
})

export default Loading; 